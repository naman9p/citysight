# CitySight Phase 1 — ANPR Engine
# Python 3.11+

# Core
numpy>=1.26
pyyaml>=6.0
pydantic>=2.6
jsonschema>=4.20

# Vision / detection / tracking
opencv-python>=4.9
ultralytics>=8.1

# OCR (PaddleOCR baseline) — pinned to known-working versions
paddleocr==3.7.0
paddlepaddle==3.3.1

# Testing
pytest>=8.0
